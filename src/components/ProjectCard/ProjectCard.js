import React from 'react'
import { ProjectCardWrapper } from './projectCardStyles'

const ProjectCard = ({ children }) => {
  return <ProjectCardWrapper>{children}</ProjectCardWrapper>
}

export default ProjectCard
